# 本版本说明

## 搜索功能升级

- 搜索接口切换为视频类型搜索，结果更稳定、更适合视频列表展示。
- 搜索结果支持分页加载，滚动到底部自动加载更多。
- 搜索历史改为本地持久化保存，最多保留 12 条。
- 搜索结果会显示当前已加载数量，并区分未搜索、无结果、加载中等状态。
- 搜索结果标题会清理高亮 HTML 标记和常见实体字符。

## 打包说明

- GitHub Actions 会构建 Release 配置的未签名 IPA。
- 生成的 `ccbili-unsigned.ipa` 会作为 Release 附件上传。