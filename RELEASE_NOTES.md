## 本版本修复

- 1080P+ 已确认能取到 `selected=116/112` 后，针对 libmpv 黑屏问题切换播放路径。
- DASH 分离音视频源改为优先交给 KSPlayer 播放，并通过 `set(urls:)` 同时传入视频 URL 和音频 URL。
- KSPlayer 路径暂时关闭硬解，优先验证 1080P+/60fps 是否能正常出画。
- 保留播放诊断开关，继续显示 `codec=`、`codecs=[...]`、DASH 列表等信息。

## 说明

请测试 1080P+ / 1080P60 是否出画和是否有声音。如果有画无声，下一步专门接 KSPlayer 音轨选择；如果仍黑屏，继续回传诊断文本。
