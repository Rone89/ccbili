## 本版本修复

- 修复 libmpv 播放视图未绑定到当前视频窗口的问题，避免 1080P 及以上 DASH 源无法正常出画。
- libmpv 改为在拿到 `UIView.layer` 后再初始化，并通过 `wid` 绑定渲染目标。
- 保留 1080P+ 优先策略，不存在时自动降级到 1080P、720P 等可用清晰度。

## 说明

本版重点修复上一版 libmpv 首次接入后的渲染目标问题；如果真机仍无法播放 1080P+，下一步需要在播放页显示接口实际返回的清晰度和 mpv 错误日志，判断是账号权限/取流降级还是解码失败。
