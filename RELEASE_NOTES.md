## 本版本修复

- 修复 1080P+ 无法切换的问题：默认 1080P 仍使用有声合流 `durl`，但会额外请求 DASH 接口补全 1080P+ / 1080P60 等清晰度选项。
- 手动选择高码率时按所选清晰度筛选 DASH 视频轨，避免总是取最高轨或取错轨。
- 保留上个版本的本地下载合流 MP4 逻辑，高码率首次切换会先下载并合流，之后复用缓存。

## 说明

如果菜单仍没有 1080P+，通常表示当前账号/视频/API 返回的 `accept_quality` 不包含该清晰度；如果菜单有但切换失败，再看具体是下载失败还是本地合流失败。
