## 本版本修复

- 保留已验证的 1080P+ 取流逻辑：已登录时不再携带 `try_look=1`，避免接口降级。
- 新增“我的 → 播放诊断 → 显示播放诊断信息”开关，默认关闭。
- 开启诊断后，播放器顶部显示取流接口、实际清晰度、Cookie 状态和 DASH 列表，便于后续排查加载失败或清晰度异常。
- 关闭诊断时不再显示黄色调试文本，恢复正常播放器界面。

## 说明

以后遇到视频加载失败或清晰度异常，可以先到“我的”页面打开播放诊断，再把播放器顶部诊断文本发出来定位。
